
/*
struct Process {
    int id;
    int size;
    int isAllocated; // 0 for No, 1 for Yes
};

int bestIdx = -1;
for (int j = 0; j < numBlocks; j++) {
    if (blocks[j].size >= process.size) { // Does it fit?
        if (bestIdx == -1 || blocks[j].size < blocks[bestIdx].size) { 
            // Is it the "Best" (smallest) so far?
            bestIdx = j;
        }
    }
}

#include <stdlib.h> // Required for abs()

int distance = abs(current_pos - next_track);
total_seek_time += distance;


// Standard Bubble Sort - the "quick and dirty" way for exams
for (int i = 0; i < n - 1; i++) {
    for (int j = 0; j < n - i - 1; j++) {
        if (req[j] > req[j+1]) {
            int temp = req[j];
            req[j] = req[j+1];
            req[j+1] = temp;
        }
    }
}
*/    